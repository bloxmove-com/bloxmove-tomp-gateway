export enum DoorStatus {
  OPEN = 'OPEN',
  CLOSED = 'CLOSED',
}

export enum VehicleType {
  CAR = 'CAR',
  SCOOTER = 'SCOOTER',
}

export enum TompBookingState {
  CONFIRMED = 'CONFIRMED',
  PENDING = 'PENDING',
}

export enum TompUnitType {
  MINUTE = 'MINUTE',
  KM = 'KM'
}
